<IfModule mod_rewrite.c>
  RewriteEngine On
  # Tanpa RewriteBase, agar universal

  RewriteCond %{REQUEST_FILENAME} !-f
  RewriteCond %{REQUEST_FILENAME} !-d
  RewriteRule ^(.*)$ index.php [QSA,L]
</IfModule>
